// worker/data/recursos-fixos.ts
//
// Quantidades diárias FIXAS que o jogo dá igual pra todo mundo —
// NÃO são preenchidas pelo usuário. Editar aqui se o jogo mudar
// essas quantias.

const recursosFixos = {
  chaveMasmorra: {
    quantidadePorDia: 8,
  },
} as const;


export default recursosFixos;